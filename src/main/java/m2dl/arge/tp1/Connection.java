package m2dl.arge.tp1;


import java.util.List;


public class Connection {
	public static void main(String args[]) {
		/*Identifier domainIdentifier = Identifier.byName("cloudmip.univ-tlse3.fr");
		OSClient os = OSFactory.builderV3()
		                       .endpoint("http://127.0.0.1:5000/v3")
		                       .credentials("ens7","M5BP4K", domainIdentifier)
		                       .authenticate();
		List<? extends Image> images = os.images().list();
		System.out.println(images.get(1).toString());*/
	}
	
}
